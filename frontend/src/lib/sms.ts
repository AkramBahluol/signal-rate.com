export type SmsResult = { encoding: "GSM-7" | "Unicode (UCS-2)"; characters: number; unicodeUnits: number; septets: number; segments: number; remaining: number; perSegment: number; extensions: number };
const basic = new Set(Array.from("@£$¥èéùìòÇ\nØø\rÅåΔ_ΦΓΛΩΠΨΣΘΞ\fÆæßÉ !\"#¤%&'()*+,-./0123456789:;<=>?¡ABCDEFGHIJKLMNOPQRSTUVWXYZÄÖÑÜ§¿abcdefghijklmnopqrstuvwxyzäöñüà"));
const extension = new Set(Array.from("^{}\\[~]|€"));
export function analyzeSms(message: string): SmsResult { let septets = 0, extensions = 0, gsm = true; for (const c of Array.from(message)) { if (basic.has(c)) septets++; else if (extension.has(c)) { septets += 2; extensions++; } else { gsm = false; break; } } const characters = Array.from(message).length; const unicodeUnits = message.length; const length = gsm ? septets : unicodeUnits; const single = gsm ? 160 : 70; const multi = gsm ? 153 : 67; const segments = length === 0 ? 0 : length <= single ? 1 : Math.ceil(length / multi); const perSegment = segments > 1 ? multi : single; const used = segments > 1 ? (length % multi || multi) : length; return { encoding: gsm ? "GSM-7" : "Unicode (UCS-2)", characters, unicodeUnits, septets, segments, remaining: perSegment - used, perSegment, extensions }; }

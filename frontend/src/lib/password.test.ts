import{describe,expect,it}from"vitest";import{entropyBits,passwordSpace,securePassword}from"./password";describe("password",()=>{it("honors constraints without Math.random",()=>{const space=passwordSpace(["upper","numbers"],true);const p=securePassword(16,space,()=>7);expect(p).toHaveLength(16);expect([...p].every(c=>space.includes(c))).toBe(true);expect(space).not.toMatch(/[0O1I]/)});it("computes entropy",()=>expect(entropyBits(10,2)).toBe(10))});

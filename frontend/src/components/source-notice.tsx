import type {SourceFields} from "@/lib/types";
import{verificationFor}from"@/lib/telecom-utils";
export function SourceNotice({record}:{record:SourceFields}){const state=verificationFor(record.verification_status);const tone=state.tone==="emerald"?"border-emerald-200 bg-emerald-50":state.tone==="rose"?"border-rose-200 bg-rose-50":state.tone==="slate"?"border-slate-200 bg-slate-50":"border-amber-200 bg-amber-50";return <aside className={`rounded-2xl border p-4 text-sm ${tone}`}><strong>{state.label}</strong><p className="mt-1 text-slate-600">{state.description} {record.last_verified_at&&<>Last checked {new Date(record.last_verified_at).toLocaleDateString("en",{dateStyle:"medium"})}. </>}{record.source_url&&<a href={record.source_url} rel="nofollow noopener" target="_blank" className="font-semibold text-[#315efb]">View {record.source_name??"source"} ↗</a>}</p></aside>}

import type{Metadata}from"next";import Link from"next/link";import{Header,Footer}from"@/components/site-chrome";import{MccLookup}from"@/components/mcc-lookup";import{Pagination}from"@/components/pagination";import{apiGet}from"@/lib/api";import{safePage}from"@/lib/telecom-utils";import type{Network,PaginatedResponse}from"@/lib/types";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"MCC / MNC Database",description:"Look up regulator-sourced mobile country codes and mobile network codes with verification details.",alternates:{canonical:"/mcc"}};export default async function Page({searchParams}:{searchParams:Promise<{page?:string}>}){const page=safePage((await searchParams).page);const result=await apiGet<PaginatedResponse<Network>>(`/mcc?page=${page}&per_page=25`);return <><Header/><main className="shell py-16"><nav className="text-sm text-slate-500"><Link href="/">Home</Link> / MCC/MNC</nav><h1 className="mt-5 text-4xl font-bold">MCC / MNC directory</h1><p className="mt-3 max-w-2xl text-slate-600">MCC identifies a country or territory; MNC identifies a network assignment within it. Leading zeros are significant and retained exactly.</p><MccLookup/><section className="mt-12"><h2 className="text-2xl font-bold">Verified network assignments</h2>{result.data.length?<div className="mt-4 grid gap-3 sm:grid-cols-2">{result.data.map(n=><Link href={`/mcc/${n.mcc}/${n.mnc}`} key={`${n.mcc}-${n.mnc}`} className="rounded-2xl border bg-white p-5"><b>{n.mcc} / {n.mnc}</b><span className="ml-3 text-slate-600">{n.assignment_name??"Unknown assignment"}</span><span className="mt-2 block text-xs uppercase text-emerald-700">{n.verification_status}</span></Link>)}</div>:<p className="mt-4 rounded-2xl border bg-white p-5 text-slate-600">No reviewed assignments are available.</p>}<Pagination meta={result.meta} path="/mcc"/></section></main><Footer/></>}

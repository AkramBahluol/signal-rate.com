import type{Metadata}from"next";import Link from"next/link";import{Footer,Header}from"@/components/site-chrome";import{networkTools}from"@/lib/network-tools";
export const metadata:Metadata={title:"Network Intelligence Tools",description:"IP, DNS, email security, web diagnostics and network calculators with privacy-conscious validation.",alternates:{canonical:"/network"},openGraph:{title:"Network Intelligence Tools | SignalRate",description:"Reliable public-network diagnostics and local calculators.",url:"/network",type:"website"}};
export default function Page(){const groups=[...new Set(networkTools.map(item=>item.group))];const schema={"@context":"https://schema.org","@type":"CollectionPage",name:"SignalRate Network Intelligence",url:"https://signal-rate.com/network"};return <><Header/><main className="shell py-14"><nav className="text-sm text-slate-500"><Link href="/">Home</Link> / Network</nav><header className="mt-7 max-w-3xl"><p className="font-bold uppercase tracking-[.18em] text-[#315efb]">Network intelligence</p><h1 className="mt-3 text-4xl font-bold tracking-tight sm:text-5xl">Understand addresses, DNS, email and the web</h1><p className="mt-5 text-lg leading-8 text-slate-600">Public-network diagnostics and local calculators with explicit limits, source-aware results, and SSRF-safe outbound checks.</p></header><div className="mt-12 space-y-12">{groups.map(group=><section key={group}><h2 className="text-2xl font-bold">{group}</h2><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{networkTools.filter(item=>item.group===group).map(item=><Link key={item.path} href={item.path} className="rounded-2xl border bg-white p-6 shadow-sm transition hover:-translate-y-0.5 hover:border-blue-300"><h3 className="font-bold">{item.name}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{item.description}</p>{item.local&&<span className="mt-3 inline-block text-xs font-semibold text-emerald-700">Runs locally</span>}</Link>)}</div></section>)}</div><section className="mt-12 rounded-3xl bg-[#10213b] p-7 text-white"><h2 className="text-2xl font-bold">Safe by default</h2><p className="mt-3 max-w-3xl text-slate-200">Lookup history is not saved by default. Outbound tools reject private, reserved, internal, and cloud metadata targets and revalidate every redirect.</p></section><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></main><Footer/></>}

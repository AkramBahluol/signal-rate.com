import{NetworkToolPage}from"@/components/network/network-tool-page";import{networkMetadata}from"@/lib/network-metadata";export const metadata=networkMetadata("Redirect Checker","Trace a safely bounded public HTTP redirect chain.","/network/redirect-checker");export default function Page(){return <NetworkToolPage title="Redirect Checker" description="Follow up to five public redirects while validating every destination against SSRF protections." mode="redirect"/>}

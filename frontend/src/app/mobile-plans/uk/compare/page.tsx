import type{Metadata}from"next";import Link from"next/link";import{Header,Footer}from"@/components/site-chrome";import{PlanTable}from"@/components/plan-comparison-table";import{apiGet}from"@/lib/api";import type{MobilePlan}from"@/lib/types";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Compare UK Mobile Plans Side by Side",description:"Compare selected UK mobile plans across cost, data, network features and verification.",alternates:{canonical:"/mobile-plans/uk/compare"},robots:{index:false,follow:true}};
export default async function Page({searchParams}:{searchParams:Promise<{plans?:string}>}){const raw=(await searchParams).plans??"";const ids=raw.split(",").map(Number).filter(Number.isInteger).filter(id=>id>0).slice(0,4);const plans=(await Promise.all(ids.map(id=>apiGet<{data:MobilePlan}>(`/mobile-plans/${id}`).then(r=>r.data).catch(()=>null)))).filter((p):p is MobilePlan=>p!==null&&p.country.iso2==="GB");return <><Header/><main className="shell py-14"><nav className="text-sm text-slate-500"><Link href="/mobile-plans/uk">UK mobile plans</Link> / Compare</nav><h1 className="mt-6 text-4xl font-bold">Compare plans side by side</h1><p className="mt-3 text-slate-600">This is a factual comparison, not a recommendation or endorsement.</p><div className="mt-8">{plans.length>=2?<PlanTable plans={plans}/>:<div className="rounded-2xl border bg-white p-6"><h2 className="font-bold">Choose two to four plans</h2><p className="mt-2 text-slate-600">Return to the UK results and select plans using the Compare buttons.</p><Link href="/mobile-plans/uk" className="mt-4 inline-block font-semibold text-[#315efb]">Browse UK plans</Link></div>}</div></main><Footer/></>}

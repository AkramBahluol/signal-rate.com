"use client";import Link from"next/link";import{useEffect}from"react";export default function ErrorPage({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{if(process.env.NODE_ENV!=="production")console.error(error)},[error]);return <main id="main-content" className="shell flex min-h-screen items-center justify-center py-20"><section className="max-w-xl rounded-3xl border bg-white p-8 text-center shadow-sm"><p className="text-sm font-bold uppercase tracking-widest text-rose-700">Temporary error</p><h1 className="mt-3 text-3xl font-bold">SignalRate could not load this page</h1><p className="mt-4 leading-7 text-slate-600">No sensitive diagnostic details are shown. Try again, or return to the homepage if the problem continues.</p><div className="mt-7 flex justify-center gap-3"><button onClick={()=>reset()} className="rounded-xl bg-blue-600 px-5 py-3 font-semibold text-white">Try again</button><Link href="/" className="rounded-xl border px-5 py-3 font-semibold">Home</Link></div></section></main>}

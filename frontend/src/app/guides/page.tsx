import type{Metadata}from"next";import Link from"next/link";import{Footer,Header}from"@/components/site-chrome";import{guides}from"@/lib/authority-content";import{jsonLd}from"@/lib/json-ld";import{siteUrl}from"@/lib/site";
export const metadata:Metadata={title:"Internet and technology guides",description:"Evidence-aware guides to internet speed, IP addresses, JSON, password security, time zones, exchange rates, TLS and DNS.",alternates:{canonical:"/guides"}};
export default function Page(){const schema={"@context":"https://schema.org","@type":"CollectionPage",name:"SignalRate guides",url:`${siteUrl}/guides`,hasPart:guides.map(g=>({"@type":"Article",name:g.title,url:`${siteUrl}/guides/${g.slug}`}))};return <><Header/><main className="shell py-12 sm:py-16"><nav className="text-sm text-slate-500"><Link href="/">Home</Link> / Guides</nav><h1 className="mt-5 text-4xl font-bold">Practical internet and technology guides</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-slate-600">Clear explanations of the measurements, formats and public data behind SignalRate&apos;s tools—including their limits.</p><div className="mt-9 grid gap-4 md:grid-cols-2">{guides.map(g=><article key={g.slug} className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold"><Link className="hover:text-[#315efb]" href={`/guides/${g.slug}`}>{g.title}</Link></h2><p className="mt-3 leading-7 text-slate-600">{g.description}</p><Link className="mt-4 inline-block font-semibold text-[#315efb]" href={`/guides/${g.slug}`}>Read guide →</Link></article>)}</div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:jsonLd(schema)}}/></main><Footer/></>}

import type{Metadata}from"next";import{LegalPage,PolicySection}from"@/components/legal-page";import{contactEmail}from"@/lib/site";export const metadata:Metadata={title:"Contact SignalRate",description:"How to contact SignalRate about corrections, privacy, security, or general feedback.",alternates:{canonical:"/contact"}};export default function Page(){return <LegalPage title="Contact SignalRate" intro="Use the configured contact channel for data corrections, privacy requests, responsible security reports, or product feedback."><PolicySection title="Contact channel">{contactEmail?<p>Email <a className="font-semibold text-blue-700 underline" href={`mailto:${contactEmail}`}>{contactEmail}</a>. Please do not include passwords, private keys, JWTs, or other secrets.</p>:<p>The public contact address has not yet been configured. The operator must set <code>NEXT_PUBLIC_CONTACT_EMAIL</code> before launch; no insecure mail relay or placeholder inbox is exposed.</p>}</PolicySection><PolicySection title="What to include"><p>For a data correction, include the affected public URL and an authoritative source. For a security report, describe reproducible impact without scanning unrelated systems or accessing other users’ data.</p></PolicySection><PolicySection title="Spam protection"><p>No contact form currently exists, so SignalRate does not collect form submissions or require a cookie-based challenge. If a form is introduced later, it must add rate limiting, validation, abuse controls, and a reviewed mail provider.</p></PolicySection></LegalPage>}

import type{Metadata}from"next";import Link from"next/link";import{LegalPage,PolicySection}from"@/components/legal-page";export const metadata:Metadata={title:"Data Methodology & Verification Policy",description:"How SignalRate sources, verifies, labels, and refreshes telecom and technical reference data.",alternates:{canonical:"/methodology"}};export default function Page(){return <LegalPage title="Data methodology" intro="SignalRate prefers authoritative, attributable sources and exposes uncertainty instead of manufacturing coverage."><PolicySection title="Source preference"><p>Standards bodies, number and telecom regulators, regional internet registries, official vendor documentation, and carrier-published material are preferred. Source access, permitted reuse, retrieval time, and transformations are documented in <Link className="font-semibold text-blue-700 underline" href="/data-sources">data sources</Link>.</p></PolicySection><PolicySection title="How major tools work"><p>The speed test loads Cloudflare&apos;s official browser package only after Start; traffic goes browser-to-Cloudflare and results are not stored by SignalRate. Currency conversion uses cached ECB reference rates and displays their reference date. Passwords use <code>crypto.getRandomValues</code> with rejection sampling in the browser.</p><p>Network diagnostics send only the requested public target to the backend. Destination validation, DNS re-checking, restricted protocols and private/reserved-address blocking enforce SSRF protections. These checks are observations from one location and time.</p></PolicySection><PolicySection title="Verification states"><p><strong>Verified</strong> means reviewed against the cited source. <strong>Unverified</strong> has not completed that review. <strong>Stale</strong> needs refresh, <strong>conflicted</strong> has unresolved source disagreement, and <strong>deprecated</strong> is retained for historical context. Weak records are excluded from indexing where appropriate.</p></PolicySection><PolicySection title="No fabricated data"><p>Unknown capabilities, prices, operators, locations, or identifiers remain unknown. One missing source observation does not delete a record; repeated absence is tracked and audited. The <Link className="font-semibold text-blue-700 underline" href="/data/mcc-mnc">MCC/MNC coverage page</Link> states the current partial scope.</p></PolicySection><PolicySection title="Domain-specific limits"><p>IP geolocation is approximate. MCC/MNC describes assignments, not necessarily retail brands. Error documentation cites standards or vendors. Mobile plan freshness depends on configured approved sources and should be verified before purchase.</p><p>See the <Link className="font-semibold text-blue-700 underline" href="/privacy">privacy policy</Link> for processing behavior.</p></PolicySection></LegalPage>}
